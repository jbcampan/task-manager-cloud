apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frontend
  namespace: task-manager
spec:
  selector:
    matchLabels:
      app: frontend
  namespaceSelector:
    matchNames:
      - task-manager
  endpoints:
    - port: http
      path: /api/metrics
      interval: 30s
