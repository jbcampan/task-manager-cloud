# Base URL of the NestJS backend API (see apps/backend)
# No NEXT_PUBLIC_ prefix: read only from Server Actions and Server Components,
# never exposed to client-side JS.
API_URL=http://localhost:3001/api/v1