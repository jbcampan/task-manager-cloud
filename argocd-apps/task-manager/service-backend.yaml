apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: task-manager
  # Required by the ServiceMonitor to select this Service for scraping.
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 3001
      targetPort: 3001
